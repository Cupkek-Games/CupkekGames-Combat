// Replaced by CombatUnitView (CupkekGames.Combat.CombatUnitView : CupkekGames.Units.UnitView).
// The class definition has been removed; this file is kept only to preserve the .meta GUID
// while prefabs are migrated to CombatUnitView in the Unity Editor.
//
// USER cleanup steps (Unity Editor):
//   1. Replace any remaining CombatUnitGameObject component on prefabs with CombatUnitView.
//   2. Delete this file (CombatUnitGameObject.cs) and its .meta sibling.
